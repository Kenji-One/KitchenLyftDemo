import { Typography, Box } from "@mui/material";

const Orders = () => {
  return (
    <Box>
      <Typography variant="h5">Orders</Typography>
      {/* Add the content for the Orders section here */}
    </Box>
  );
};

export default Orders;
